# Copy to .env at repo root. Never commit. Each key lives with the ONE agent that calls it.
TAVILY_API_KEY=            # Agent B (roster) + Agent D (enrichment gap-fill)
NEBIUS_API_KEY=            # Agent C (priorities + judge). Token Factory serverless, no GPU.
IRIDIUM_API_KEY=           # Agent D. irid_... from Iridium Settings -> rotate-api-key
RUNTYPE_API_KEY=           # stretch: Mechanism B eval suite
COTAL_LINK=                # cotals://<token>@host:4222/prep-fleet?channel=prep
TELEGRAM_BOT_TOKEN=        # HITL bridge; falls back to reading token.txt
TELEGRAM_CHAT_ID=          # auto-discovered by scripts/hitl.py discover
